#include <stdio.h>

// 주석 설정 단축키 : ctrl + k + c  ctrl + shift + slash
// 주석 해제 단축기 : ctrl + k + u
// 다중라인 선택 단축키 : Alt + Shift 한 채로 키보드 화살표 방향키 또는 마우스 클릭 / 드래그

// /* 와 */ 사이 내용 컴파일러 시 무시

// 전처리기 클릭하고 F12 누르면 해당 전처리기 소스파일 세부내용 볼 수 있음


int main(void)
{

	printf("%d", sizeof(1));

	return 0;
}